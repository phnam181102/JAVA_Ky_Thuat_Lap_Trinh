# Ky Thuat Lap Trinh

This repository content code of programing using java.

## How to use git
dir -> git status -> git add . -> git commit -m "" -> git push
```
$ git status
$ git init 
$ git push
$ git pull 
$ git commit -m 'update'
```
